
import {
  Button,
  buttonVariants,
} from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
    <div>
   <Button className="mt-40" variant="destructive" size="default">
    Deepak build the fucking app bro
   </Button>
    </div>
  );
}
